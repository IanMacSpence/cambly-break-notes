{
  "name": "cambly-break-notes",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview", 
    "build:userscript": "vite build --mode userscript",
    "build:userscript:watch": "vite build --mode userscript --watch"
  },
  "devDependencies": {
    "vite": "^7.2.4"
  }
}
